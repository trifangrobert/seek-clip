export type UserProfile = {
    email: string,
    firstName: string,
    lastName: string
}

export type UserProfileToken = {
    email: string,
    firstName: string,
    lastName: string,
    token: string
}